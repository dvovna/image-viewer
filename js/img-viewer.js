window.IV = {};

(function () {
    'use strict';

    window.ImageViewer = Backbone.Router.extend({
        initialize: function (options) {
            this.options = options || {};
            console.log('start');
        }
    })
}());